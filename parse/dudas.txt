1) time y to son palabras reservadas y en algunos ejemplos actuan como id, 
hay que aceptar que los nombres de tabla,columna… 
pueden ser palabras reservadas cmd(to, table) y function(lenght)

2) Como por ejemplo en eso dos sentencias
create table flights(airline string,frm string,to string,departs int,arrives int);
CREATE TABLE flight(origin string, destination string, time real); 

3) Optional_referential_triggered_action no se para que sirve, porque no veo ningun utilidad.

4) Join relacion tengo un problema que no he podido resolver
estos cuatros casos sale correcto
select * from t join s join u;
select * from (t join s join u);
select * from t join (s join u);
select * from t join s join u;

pero cuando el parentesis aparca primer join y dejando el segundo join fuera sale error.
que es muy probable que sea un error de corte(!).
Al hacer debug creo sale el error porque en select_DQL
consume un parentesis y en el predica relation ya solo queda parentesis de cierre y por eso
sale error, pero no estoy seguro. 
--select * from (t join s) join u;
 
5) He ignorado la mayoria de los Syntax check ya que depende de bastantes otros predicaods:
syntax_check_redef
syntax_check_same_types

6) Tampoco el predicado create_or_replace_view() dentro del HR-SQL CREATE VIEW syntax
tambien porque necesitan muchos predicados que desconozco la funcionalidad.

lo que me falta son: 
1) Algunos SQL Expressions Factor
2) Cambiar algunos cmd(replace, float, left, right) en lexer ya que algunos de ellos puede ser cmd y tambien fn
3) Los fn pueden ser tambien e y pi que son id o id_lc_start