1) time y to son palabras reservadas cm en mi caso y en algunos ejemplos actuan como id, 
¿debo aceptar que los nombres de tabla,columna...
pueden ser palabras reservadas cmd(to, table) y function(lenght)?

Como por ejemplo en eso dos sentencias
create table flights(airline string,frm string,to string,departs int,arrives int);
CREATE TABLE flight(origin string, destination string, time real); 

2) Optional_referential_triggered_action no se para que sirve, porque no veo ningun utilidad por ahora.

3) En join relacion tengo un problema que no he podido resolver
estos cuatros casos sale correcto
select * from t join s join u;
select * from (t join s join u);
select * from t join (s join u);
select * from t join s join u;

Pero cuando el primer join esta dentro de parentesis y dejando el segundo join fuera sale error.
Que es muy probable que sea un error de corte(!).
Al hacer debug creo sale el error porque en select_DQL
consume el parentesis de apertura y en el predicado relations ya solo queda parentesis de cierre y por eso
sale error, pero no estoy seguro. 
--select * from (t join s) join u;

4) En estos casos sale error, creo que es poruqe no son de mismo tipo.
--select date '2017-02-01' - 1
--select cast((datetime '1-1-1 0:0:0' - 1) as string);

5) He ignorado la mayoria de los Syntax check ya que depende de bastantes otros predicados:
-syntax_check_redef
-syntax_check_same_types

6) Tampoco el predicado create_or_replace_view() dentro del HR-SQL CREATE VIEW syntax,
tambien porque necesita muchos  otros predicados que desconozco la funcionalidad.
